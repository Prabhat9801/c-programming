#include<stdio.h>
int main()
{
    int x;
    printf("Enter the value of x for the cheking of natural number : ");
    scanf("%d",&x);
    if(x>=1)
{
    printf("It is the natural number");
} else {
    printf("It is not the natural number");

} return 0;
}
